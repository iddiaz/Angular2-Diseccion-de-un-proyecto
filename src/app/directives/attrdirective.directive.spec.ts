import { AttrdirectiveDirective } from './attrdirective.directive';

describe('AttrdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AttrdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
